---
title: Analysis R package for ntels Business Intelligence Team
author: <a href = "https://lovetoken.github.io">lovetoken</a>
date: "`r Sys.Date()`"
output: 
  html_document:
    fig_height: 6
    theme: yeti
    toc: yes
    toc_depth: 3
    toc_float: yes
    keep_md: yes
---

```{r env, echo = F, warning = F, message = F}
pacman::p_load("knitr", "tidyverse", "ggplot2", "data.table")
library(ntelsBI)
opts_chunk$set(fig.path = "output/figure/", fig.align = "center", out.width = "80%", 
               warning = F, message = F)
```

<br><br>

## Tutorial

### Machine learning

`ml()` 함수를 이용하여 기계학습 훈련 및 평가를 수행할 수 있습니다.  

* CART 를 통한 `iris` 꽃잎분류

```{r}
ml(iris, "Species", method = "rpart") %>% fitSummary(type = "cla")
```

* GLM 을 통한 `mtcars` 의 `am` 리샘플링 이후의 분류

```{r}
ml(mtcars, "am", resampling = T, method = "glm") %>% fitSummary(type = "cla")
```

* Random forest 를 통한 `mtcars` mpg 예측

```{r}
ml(mtcars, "mpg", method = "rf") %>% fitSummary(type = "reg")
```

<br>

### Useful preprocessing

* `range01()` function : Standardization 0 to 100

```{r}
x <- c(0, 4, 7, 9, 10)
range01(x) * 100 
```

## Install

```{r eval = F}
devtools::install_github("ntels-BI/ntelsBI")
```

<br>

## License

[GPL-3](https://www.gnu.org/licenses/gpl-3.0.en.html)
